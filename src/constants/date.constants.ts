export enum DATE_FORMAT {
  DATE = 'YYYY-MM-DD',
  ISO_DATE_TIME = 'YYYY-MM-DDTHH:mm:ss.SSS[Z]',
}

export enum TIMEZONE {
  UTC = 'UTC',
}

export enum DATE_UNIT {
  DAY = 'day',
  MONTH = 'month',
  YEAR = 'year',
}
