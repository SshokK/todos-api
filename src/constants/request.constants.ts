export enum ROUTE_VERSION {
  V1 = '1',
}

export const ROUTE_DEFAULT_VERSION = ROUTE_VERSION.V1;

export const SWAGGER_ROUTE = 'api';

export enum HTTP_METHODS {
  CONNECT = 'CONNECT',
  DELETE = 'DELETE',
  GET = 'GET',
  HEAD = 'HEAD',
  OPTIONS = 'OPTIONS',
  PATCH = 'PATCH',
  POST = 'POST',
  PUT = 'PUT',
  TRACE = 'TRACE',
}
